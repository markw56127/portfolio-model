# Local backups of the raw Shiny downloads, kept from before the duplicate
# V (Visa) column was removed. Local safety net only.
*.bak

# R session state
.Rhistory
.RData
.Ruserdata
.Rproj.user/

# LaTeX intermediates from knitting to pdf. Not *.pdf -- the rendered report
# in Project1/Submission/ is the deliverable and is tracked.
*.tex
# ...but hand written LaTeX sources are not knitr output and must be tracked
!Homework*/*.tex
*.aux
*.toc
*.out
*.synctex.gz
*.log
*_files/

# macOS folder metadata
.DS_Store
